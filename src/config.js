const config = {
  API_BASE_URL: "https://lereacteur-vinted-api.herokuapp.com/v2", //utiliser celui ci

  // API_BASE_URL: "http://localhost:3000", //pour un serveur en local
};

export default config;
